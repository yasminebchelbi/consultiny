#ifndef INDICATEURMETEO_H
#define INDICATEURMETEO_H

#include <QObject>
#include <QNetworkAccessManager>
/*#include <QNetworkReply>
#include <QJsonDocument>
#include <QJsonObject>

class IndicateurMeteo : public QObject
{
    Q_OBJECT
public:
    explicit IndicateurMeteo(QObject *parent = nullptr);

    void recupererMeteo(const QString &ville);

signals:
    void meteoPrete(double temperature, double vitesseVent, bool pluie, QString conseil);

private slots:
    void quandMeteoRecue(QNetworkReply *reponse);

private:
    QNetworkAccessManager *gestionnaireReseau;
    QString cleAPI = " 5063bc86b16a955447023da5858cb10b";
};

#endif // INDICATEURMETEO_H
*/
